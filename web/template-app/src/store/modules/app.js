const app = {
    state: {
        printInfo: {
            tableData: [],
            tableColunms: [],
        }
        // count: 1,
        // showLeftBar: false, // 是否显示左侧菜单
    },
    mutations: {
        // add(state, num) {
        //     state.count = state.count + num
        // },
        // toggleLeftBar(state, bol) {
        //     state.showLeftBar = bol;
        // }
    }
}

export default app